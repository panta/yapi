# yaml-language-server: $schema=https://pond.audio/yapi/schema
url: https://httpbin.org/post
method: POST
content_type: application/json
body:
  samples:
    - name: "sample1"
      value: 1
    - name: "sample2"
      value: 2

jq_filter: |
  .json.samples |= map({name})
