# Test various assertion operators failing
yapi: v1
url: https://jsonplaceholder.typicode.com/todos/1
expect:
  status: 200
  assert:
    - .id > 100
    - .completed != false
    - .userId < 1
    - .id >= 10
