# yaml-language-server: $schema=https://pond.audio/yapi/schema

# gRPC request example using grpcb.in test service
# Uses server reflection - no proto files needed

url: grpc://grpcb.in:9000

service: hello.HelloService
rpc: SayHello
plaintext: true

body:
  greeting: "Emily"
